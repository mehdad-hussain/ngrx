import { JoinObjectValuesPipe } from './join-object-values.pipe';

describe('JoinObjectValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinObjectValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
